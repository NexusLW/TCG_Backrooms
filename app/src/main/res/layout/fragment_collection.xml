<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/poolrooms_bg">

    <!--header-->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Collection"
        android:textColor="@color/poolrooms_text"
        android:textSize="22sp"
        android:textStyle="bold"
        android:padding="16dp"/>

    <!--the card grid, locked cards show as ??? and unlocked show the real image-->
    <!--la cuadricula de cartas, las bloqueadas muestran ??? y las desbloqueadas la imagen real-->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        android:clipToPadding="false"/>

</LinearLayout>